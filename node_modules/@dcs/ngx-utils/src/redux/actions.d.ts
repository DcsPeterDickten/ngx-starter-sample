export interface IAsyncActionNames {
    base: string;
    start: string;
    next: string;
    error: string;
}
/**
 * Generates action names via naming convention based on [[observableMiddleware]].
 *
 * With the given baseName 'PRODUCT' it returns the object
 * ```
 * {
 *   base:  'PRODUCT',
 *   start: 'PRODUCT_START',
 *   next:  'PRODUCT_NEXT',
 *   error: 'PRODUCT_ERROR'
 * }
 * ```
 *
 * @export
 * @param  baseName
 * @returns
 */
export declare function generateAsyncActionNames(baseName: string): IAsyncActionNames;
