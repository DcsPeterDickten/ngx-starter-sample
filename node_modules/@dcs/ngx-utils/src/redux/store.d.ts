import { Store, Reducer, Middleware, GenericStoreEnhancer } from 'redux';
import { IState } from '../interfaces';
/**
 * Helper function to setup the redux store with reducers, middleware and enhancers.
 *
 * @export
 * @param  rootReducer
 * @param )]
 * @param  [middleware=[]]
 * @param  [enhancers=[]]
 * @returns
 */
export declare function createReduxStore(rootReducer: Reducer<IState>, initialState?: IState, middleware?: Middleware[], enhancers?: GenericStoreEnhancer[]): Store<IState>;
/**
 * Redux [[GenericStoreEnhancer]] that persists all state changes to localStorage,
 * knowing how to handle an ImmutableJS state.
 *
 * @export
 * @returns
 */
export declare function persistStateEnhancer(paths?: string | string[]): GenericStoreEnhancer;
/**
 * Selector function to return the current router substate.
 *
 * @export
 * @param  state
 * @returns string
 */
export declare function routerStateSelector(state: IState): string;
