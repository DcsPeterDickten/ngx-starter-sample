import { createSelector as cs } from 'reselect';
/**
 * Small wrapper around reselect [[createSelector]], that knows how to compare ImmutableJS data structures.
 */
export declare const createSelector: typeof cs;
