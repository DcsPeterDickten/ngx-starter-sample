import { IReducer, IReducerConfig } from '../interfaces';
/**
 * Class that builds the root reducer for redux from all injected [[APP_REDUCERS]].
 *
 * @export
 */
export declare class RootReducer {
    private reducerConfigs;
    private currentReducers;
    constructor(reducerConfigs: IReducerConfig[]);
    /**
     *
     *
     * @param  [newReducerConfigs]
     * @returns
     * @memberof RootReducer
     */
    reducer(newReducerConfigs?: IReducerConfig[]): IReducer;
    /**
     * Builds the root reducer.
     *
     * @param  [newReducerConfigs]
     * @returns
     * @memberof RootReducer
     */
    buildReducers(newReducerConfigs?: IReducerConfig[]): any;
}
