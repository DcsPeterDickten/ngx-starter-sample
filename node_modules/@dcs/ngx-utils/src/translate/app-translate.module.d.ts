import { TranslateService } from '@ngx-translate/core';
export declare class AppTranslateModule {
    private translate;
    private appLocale;
    private translations;
    constructor(translate: TranslateService, appLocale: string, translations: Array<{
        name: string;
        translations: any;
    }>);
    setupTranslations(): void;
}
