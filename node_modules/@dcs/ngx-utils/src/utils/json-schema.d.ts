/**
 * Given a JSON schema, this compiles a validator function to use later
 *
 * Basically a small wrapper around [[Ajv]]
 *
 * @export
 * @param  jsonSchema
 * @param  [ajv]
 * @returns
 */
export declare function buildSchemaValidator(jsonSchema: object, ajv?: any): any;
