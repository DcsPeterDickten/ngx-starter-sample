Schnellstart:

```
git clone https://github.com/dcsfuerth/dcs-ngx-sample.git
cd dcs-ngx-sample/
npm install
npm run watch (in einem Terminalfenster)
npm run backend:watch (in einem anderen Terminalfenster)
```
